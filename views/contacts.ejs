<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contact</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>
<body>
    <style>
        .message-box{
            color: #9a9393;
            font-family: sans-serif;
        }
    
    </style>
    <%- include('partial/navbarHead') %> 
    <li class="nav-item">
        <%  if(userEmail) {%>
            <a class="nav-link active" aria-current="page" href="/logout">Logout</a>
        <% }else{ %>
            <a class="nav-link active" aria-current="page" href="/">Login</a>
        <% } %>
    </li>
    <% if(userEmail){ %>
        <li class="nav-item">
            <a class="nav-link disabled" href="#"> Wellcome <%= userName%> </a>
        </li>
    <%  }%>
    <%- include('partial/navbarFoot') %> 
    <%  if(!userEmail) {%>
        <div class="d-flex justify-content-center mt-5 container-md" style="width: 400px; height: 200px;">
            <div class="message-box mt-1 w-70">
                <h1 class="text-center"> Contact Info</h1>
                <div class="mt-4 rounded border p-3 h-100">
                    <p class="text-dark">contact no : </p>
                    <p class="mt-3 text-dark">emai : </p>
                    <p class="mt-3 text-danger"> you need to login first in order to contact through app or you can try to contact through email or mobile no</p>
                </div>
            </div>
        </div>
    <%} else { %>
        <div class="d-flex justify-content-center mt-5 container-md">
            <div class="message-box mt-1 w-70">
                <h1 class="text-center"> Contact </h1>
                <form id="form" action="/contacts" method="post" onsubmit="sendMessage(event)">
                    <div class="inner-box mt-4 rounded border p-3 h-100">
                    <label for="messageField" class="mt-3 text-dark">message : </label>
                    <textarea type="text" id="messageField" name="msg" class="form-control mt-3" rows="4" required></textarea>
                    <button type="submit" class="btn btn-primary mt-3">send</button>
                    <p class="mt-3">please type your problem and we will try to help you as soon as possible</p>
                </form>
            </div>
        </div>
    <% } %>
    <script>
        function sendMessage(event)
        {
            event.preventDefault();
            document.getElementById('form').submit();
            document.getElementById('messageField').value = ''
        }
    </script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
    crossorigin="anonymous">
    </script>
</body>
</html>