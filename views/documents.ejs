<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Documents</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>
<body>
    <style>
        .form-container {
            max-width: 250px;
        }

        .heading {
            font-family: sans-serif;
            color: #9a9393;
        }

        .line {
            border: 1px solid;
        }

        .option-container {
            padding: 10px;
            margin-left: 10%;
        }

        .option-list {
            max-height: 300px;
            overflow-y: auto;
            border: 2px solid #eee;
            border-radius: 10px;
            padding-left: 10%;
            padding-right: 5%;
        }

        .message {
            font-weight: bold;
            color: black;
        }

        @media (max-width: 767px) {
            .form-container {
                max-width: 100%;
            }

            .option-container {
                max-width: 80%;
                flex-wrap: wrap;
            }

            .option-list {
                max-width: 100%;
                padding-left: 5%;
                padding-right: 5%;
            }
        }
    </style>
    <%- include('partial/navbarHead') %> 
    <li class="nav-item">
        <%  if(userEmail) {%>
            <a class="nav-link active" aria-current="page" href="/logout">Logout</a>
        <% }else{ %>
            <a class="nav-link active" aria-current="page" href="/">Login</a>
        <% } %>
    </li>
    <% if(userEmail){ %>
        <li class="nav-item">
            <a class="nav-link disabled" href="#"> Wellcome <%= userName%> </a>
        </li>
    <%  }%>
    <%- include('partial/navbarFoot') %> 
       <% if (!flag) { %>
        <div class="container-lg">
            <div class="mt-5 d-flex justify-content-center">
                <h1 class="text-danger">You need to login first!!!</h1>
            </div>
        </div> 
        <% }else { %>
            <div class="container-sm">
                <div class="mt-5">
                    <div class="d d-flex justify-content-center">
                        <h1 class="heading">Documents</h1>
                    </div>
                    <hr class="line">
                    <div class="d-flex flex-column mt-5">
                        <div>
                            <select class="form-select form-container" onchange="displayMessage()" multiple
                                aria-label="multiple select example" id="documents">
                                <option value="1">Driving License</option>
                                <option value="2">Passport</option>
                                <option value="3">Pancard</option>
                                <option value="4">Caste certificate</option>
                                <option value="5">Nationality</option>
                                <option value="6">Domicile</option>
                                <option value="7">Non-creamy layer</option>
                                <option value="8">Income Certifiate</option>
                            </select>
                        </div>
                        <div class="option-container mt-3">
                        </div>
                    </div>
                </div>
            </div>
           <% } %> 
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
                    integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
                    crossorigin="anonymous">
            </script>
            <script src="/apps/app.js"></script>
</body>

</html>